create database tareas

use tareas

create table task(
idTarea int primary key not null identity,
nombre varchar(100),
descripcion varchar(200),
prioridad varchar(50),
taskComplete int,
)



insert into task (nombre,descripcion,prioridad,taskComplete) values('Hacer prueba tecnica', 'Prueba tecnica', 'Alta','0')

select * from task